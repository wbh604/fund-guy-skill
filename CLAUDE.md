# Fund Guy Skill

基金经理行为审计 skill。完整指南见 `AGENTS.md`,方法论主文档见 `skills/fund-manager-alpha/SKILL.md`(分析任务先读它)。

- 一键运行:`python run.py <基金代码>`
- 斜杠命令:`/analyze-fund` `/quick-check` `/find-similar`(见 `commands/`)
- 硬底线:先任期切割;绝不编数;定性判断由你做,不写脚本
